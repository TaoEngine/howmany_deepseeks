name: howmany_deepseeks
description: "How many DeepSeek on your phone"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3
  http: ^1.3.0
  installed_apps:
    git:
      url: https://github.com/shatanikmahanty/installed_apps.git
  # just_audio: ^0.9.46
  toml: ^0.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - audio/congratulations.wav
    - image/background.png